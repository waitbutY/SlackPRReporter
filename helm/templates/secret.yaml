# Placeholder — real values are injected via Sealed Secrets or external-secrets operator.
# Do NOT commit real secret values here.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "slack-pr-reporter.fullname" . }}-secrets
  labels:
    {{- include "slack-pr-reporter.labels" . | nindent 4 }}
  annotations:
    # managed-by: sealed-secrets  # Uncomment if using Sealed Secrets
type: Opaque
data:
  SLACK_BOT_TOKEN: ""
  SLACK_SIGNING_SECRET: ""
  GITHUB_APP_ID: ""
  GITHUB_APP_PRIVATE_KEY: ""
  GITHUB_INSTALLATION_ID: ""
  GITHUB_WEBHOOK_SECRET: ""
